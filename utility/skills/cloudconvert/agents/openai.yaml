interface:
  display_name: "CloudConvert Document Conversion"
  short_description: "Convert approved non-text documents to Markdown through the external CloudConvert service"
  default_prompt: "Convert this approved document to Markdown with CloudConvert"
