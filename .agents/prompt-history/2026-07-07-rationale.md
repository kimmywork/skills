/skill-creator 创建一个解读专用的skill，“rationale”。放在 utility/skills 目录下。

我们整体的原则是：Agent 比人聪明，所以不需要过多的解释，需要的是具体的、准确的、可执行的、没有歧义的指令。但是人去理解这些东西的时候，是需要一个理解过程的。这个技能可以解释任意与上下文有关的内容，包括一个 SKLL、AGENTS.md的规则、以及任意当前context的输出或者项目里的概念、变更等。Agent需要结合完善的上下文查询，分析变更历史或者完整的文件结构，来给出一个表达简洁、内容详实、、信息完备、逻辑链完整闭环、使用事实支撑、分层简洁——每段内容只讲一个核心观点。

同时对用户意图进行简单分析，是需要简单的解读还是完整的方案论证，并根据用户的语言模式来控制详略程度。

另外跟我讨论几点：
- 这个技能存在的意义和价值
- 这个名字是否符合
- 这个技能是否可以用在其他地方 - 比如使用它来生成Rationale文档
- 输出的结构上应该包含什么东西，是否需要模板化，如何控制颗粒度和详略程度
- A Rationale for Rationale skill?

---

很好，你完全捕捉到了我的想法。现在我需要再额外加一些东西：（当然也需要你来帮我决定）
- Rationale 探索的文档是否可以超出 workspace，比如检索外部文档或者网络
- Rationale 的探索过程什么时候中止

---

另外再更新一个原则到 AGENTS.md: 当前目录中的SKILL（.agents/skills 下的第三方除外) 尽可交叉引用同plugin bundle下面的内容，不能包含超出bundle scope的技能、文件、引用或者功能。如果需要的东西不存在，验证：

1. 是否违反了限定scope原则，是否有超出了scope的行为
2. 是否职责过于庞大
3. 是否与项目中的其他plugin下的SKILL有交叉
4. 所请求/需要的内容是否有成为一个plugin范围内独立技能的必要
5. 是否能提供充分的用户价值。

只有上面的验证完全满足，才考虑加入一个新的SKILL。

---

plugin bundle 是指一个 plugin （.{claude,codex}-plugin/plugin.json）所在目录下面的所有 SKILL。这样这些SKILL交叉引用的时候是可以一同被安装的。

skill bundle是你说的skill所在目录和references/ scripts/。

---

重新组织一下AGENTS.md呗。

这两个定义可以提前，然后交叉检验一下每一条原则看是否重复或者相互冲突，整体重新调整一下安排的逻辑顺序和结构。Post creation checklist也更新一下检查项。

---
